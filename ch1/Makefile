#
# Generated By "genmake"
# Date : 2023.07.25
# YJYOON
#
SUBDIRS =
MAKE    = make
all:
	@for i in $(SUBDIRS); do (cd ./$$i && echo $$i: && $(MAKE) $@) done || exit 1

clean:
	@for i in $(SUBDIRS); do (cd ./$$i && echo $$i: && $(MAKE) $@) done || exit 1

compress :
	@for i in $(SUBDIRS); do (cd ./$$i && echo $$i: && $(MAKE) $@) done || exit 1
	@rm -f ./lib/*.a
	@tar -cvf ../back.tar ./

